<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.input :description, placeholder: "5 ounces" %>
<%= f.association :ingredient %>
<%= f.button :submit, class: "btn btn-primary" %>

<% end %>


 <!-- simple_form is is super smart.
 f.input :ingredient will return an ATTRIBUTE found in the schema
 (description, name)

f.association :ingredient
 ->  will look for an ASSOCIATED MODEL : eg. the column
 INGREDIENT_ID found in the table DOSES in the schema
and it can now propose a dropdown list of all ingredients



-->
